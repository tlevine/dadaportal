{# Description first so it shows up if there's no highlighting #}
{{ object.description }}

{# Then body #}
{{ object.body|striptags }}

{# Then everything else #}
{{ object.title }}
{% for tag in object.tags %}
  {{ tag }}
{% endfor %}
